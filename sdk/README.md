# AXIOM SDK

TypeScript helpers for constructing AXIOM Anchor instructions and decoding core accounts.

The SDK is intentionally thin: it wraps the generated Anchor program client and keeps PDA derivation, enum formatting, and fixed-size policy inputs consistent across app, agent, and scripts.

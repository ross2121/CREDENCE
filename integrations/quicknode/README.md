# AXIOM QuickNode Integration

This package contains AXIOM realtime RPC helpers:

- RPC and WebSocket connection config
- program log listener
- liquidation warning parser/listener
- transaction simulation helper

Tests use mocked connections, so no QuickNode endpoint is needed locally.
